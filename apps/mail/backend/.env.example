PORT=8081

# Postgres
PGHOST=postgres
PGPORT=5432
PGUSER=postgres
PGPASSWORD=postgres
PGDATABASE=pilot180mail

# Keycloak
KEYCLOAK_URL=http://keycloak:8080/
KEYCLOAK_REALM=pilot180
KEYCLOAK_CLIENT_ID=mail-backend
KEYCLOAK_CLIENT_SECRET=replace_with_secret

# SMTP (MailHog / MailDev)
SMTP_HOST=localhost
SMTP_PORT=1025
SMTP_SECURE=false
SMTP_FROM=no-reply@pilot180.local

# SMTP receiver port (for incoming email ingest)
SMTP_RECEIVER_PORT=2525

# Session secret
SESSION_SECRET=pilot180-secret
