---
# ClusterRole for reading pod metrics from Metrics Server
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: whooper-metrics-reader
  labels:
    app: whooper
    component: backend
rules:
  # Read pod metrics from Metrics Server
  - apiGroups: ["metrics.k8s.io"]
    resources: ["pods"]
    verbs: ["get", "list"]

  # Read node metrics (optional, for future use)
  - apiGroups: ["metrics.k8s.io"]
    resources: ["nodes"]
    verbs: ["get", "list"]

---
# Bind the metrics reader role to whooper-backend ServiceAccount
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: whooper-metrics-reader-binding
  labels:
    app: whooper
    component: backend
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: whooper-metrics-reader
subjects:
  - kind: ServiceAccount
    name: whooper-backend
    namespace: whooper
