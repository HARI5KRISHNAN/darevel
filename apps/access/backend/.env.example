# Database Configuration
DB_URL=jdbc:postgresql://localhost:5447/access
DB_USERNAME=access
DB_PASSWORD=access

# Redis Configuration
REDIS_HOST=localhost
REDIS_PORT=6387

# OAuth2 / JWT Configuration
JWT_ISSUER_URI=http://localhost:8180/realms/darevel

# Service Configuration
SERVER_PORT=8096
SPRING_PROFILES_ACTIVE=dev

# Access Control Configuration
PERMISSION_CACHE_TTL=PT10M
ASSIGNMENT_CACHE_TTL=PT5M

# Redis Channels
REDIS_ASSIGNMENT_CHANNEL=access.assignment.events
REDIS_PERMISSION_CHANNEL=access.permission.events

# Default Roles
BOOTSTRAP_ROLES=workspace-admin,workspace-editor,workspace-viewer
FALLBACK_ROLE=workspace-viewer
