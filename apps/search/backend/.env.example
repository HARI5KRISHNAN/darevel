# Database Configuration
DB_URL=jdbc:postgresql://localhost:5446/search
DB_USERNAME=search
DB_PASSWORD=search

# Redis Configuration
REDIS_HOST=localhost
REDIS_PORT=6386

# Meilisearch Configuration
MEILISEARCH_HOST=http://localhost:7710
MEILISEARCH_API_KEY=masterKey

# OAuth2 / JWT Configuration
JWT_ISSUER_URI=http://localhost:8180/realms/darevel

# Service Configuration
SERVER_PORT=8095
SPRING_PROFILES_ACTIVE=dev

# Search Configuration
SEARCH_DEFAULT_LIMIT=20
SEARCH_MAX_LIMIT=100
SEARCH_HIGHLIGHT=true

# Index Names
INDEX_DOCUMENTS=darevel_documents
INDEX_FILES=darevel_files
INDEX_FORMS=darevel_forms
INDEX_WIKI_PAGES=darevel_wiki
INDEX_EMAILS=darevel_emails
INDEX_MESSAGES=darevel_messages

# Redis Channels
REDIS_INDEX_CHANNEL=search.index.events
REDIS_WORKSPACE_CHANNEL=workspace.events
