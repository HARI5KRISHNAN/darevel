# Darevel Suite - Docker Compose
# Currently minimal - add services as needed

version: '3.8'

services:
  # Add services here as needed
  # Example: databases, caching, message queues, etc.

# Uncomment when services are added
# volumes:
#   data:

# Uncomment when custom networks are needed
# networks:
#   darevel:
